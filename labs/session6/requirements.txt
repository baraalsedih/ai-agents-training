# ============================================================
# Lab environment requirements — Session 6: Evaluation, Cost & Observability
# Install these inside a virtual environment (see README.md)
#   pip install -r requirements.txt
# Same pinned stack as labs/session5/ (evaluate.py imports and runs that
# team's real graph directly, so its dependencies must match exactly),
# plus PyYAML to read golden_set.yaml.
# Everything here runs 100% locally through Ollama — no API keys needed.
# ============================================================

langchain==1.3.13
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9
pypdf==6.14.2
docx2txt==0.9
pydantic==2.13.4
numpy==2.5.1

langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0

pyyaml==6.0.3
